{% extends 'base.html.twig' %}

{% block title %}Hello AuthentificationController!{% endblock %}

{% block body %}

	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}

		form {

			margin-top: 40px;
		}
		button[type="submit"] {
			padding: 10px;
			margin-top: 50px;
			width: 50;
            text-align:center;
		}
	</style>

	<div class="example-wrapper">
        <h2 style="text-align:center">Authentification</h2>
        <form role="form" action="{{url("authentification")}}" method="POST" class="text-start">
            <label class="form-label">Nom d'utilisateur</label>
            <input type="text" name="_username" id="username" value="{{last_username}}" class="form-control"/>

            <label class="form-label">Mot de passe</label>
            <input type="password" name="_password" id="password" class="form-control">

            <input type="hidden" name="_target_path" value="{{url('liste_film')}}">

		    <button type="submit" class="btn btn-primary">Se connecter</button>
            <p class="mt-4 text-sm text-center">
            Avez-vous un compte?
                <a href="{{url("inscription")}}" class="text-primary text-gradient font-weight-bold">Créer un compte</a>
            </p> 
            </form>
	</div>
{% endblock %}
